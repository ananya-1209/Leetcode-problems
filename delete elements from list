#include <stdio.h>

int main() {
    int N, deleteElement;

    // Read the size of the list
    scanf("%d", &N);

    // Declare an array of size N
    int arr[N];

    // Read N elements into the array
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    // Read the element to be deleted
    scanf("%d", &deleteElement);

    // Find the first occurrence of the deleteElement
    int i;
    for (i = 0; i < N; i++) {
        if (arr[i] == deleteElement) {
            // Shift elements to the left
            for (int j = i; j < N - 1; j++) {
                arr[j] = arr[j + 1];
            }
            N--;  // Decrease the size of the array
            break;  // Stop after deleting the first occurrence
        }
    }

    // Print the updated list
    for (int i = 0; i < N; i++) {
        printf("%d", arr[i]);
        if (i < N - 1) {
            printf(" ");  // Print space between elements
        }
    }

    return 0;
}

